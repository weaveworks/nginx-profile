apiVersion: profiles.fluxcd.io/v1alpha1
kind: Profile
metadata:
  name: nginx
spec:
  description: Profile for deploying nginx
  maturity: alpha
  owner: WeaveWorks <gitops@weave.works>
  version: v0.0.1
  artifacts:
    - name: nginx-server
      path: nginx/chart
